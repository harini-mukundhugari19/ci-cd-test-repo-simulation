name: Out of Memory (OOM) Simulation

on:
  push:
    branches:
      - test-ambiguous-errors
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Run OOM Simulation Script
        run: python oom_crash.py
